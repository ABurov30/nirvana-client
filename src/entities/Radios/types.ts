export type RadioType = {
  id: string;
  name: string;
  url: string;
  homepage: string;
  country: string;
  favicon: string;
  tags: ['', ''];
  language: ['', ''];
  votes: number;
  codec: string;
};

export type ApiResponse = {};
